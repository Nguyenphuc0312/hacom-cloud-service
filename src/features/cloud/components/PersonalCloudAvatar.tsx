import { CloudIcon } from '@heroicons/react/24/outline'; const sizeClass = { sm: 'h-9 w-9', md: 'h-10 w-10', lg: 'h-16 w-16' } as const; export const PersonalCloudAvatar = ({ size = 'md', className = '' }: { size?: keyof typeof sizeClass; className?: string }) => (<span role="img" aria-label="Hacom Cloud" className={['grid shrink-0 place-items-center rounded-full bg-brand-soft text-brand-solid', sizeClass[size], className].filter(Boolean).join(' ')}><CloudIcon className={size === 'lg' ? 'h-8 w-8' : 'h-5 w-5'} aria-hidden="true" /></span>); export default PersonalCloudAvatar;