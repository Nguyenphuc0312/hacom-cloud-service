import React from "react";

interface BlockedUsersSectionProps {
  id?: string;
}

// Disabled — block user feature removed for internal chat.
export const BlockedUsersSection: React.FC<BlockedUsersSectionProps> = () => null;

export default BlockedUsersSection;
