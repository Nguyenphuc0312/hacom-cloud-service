# Database migrations

Thư mục chứa migration PostgreSQL theo cặp `*.up.sql` và `*.down.sql`.

Schema chính thức cần được nhóm chốt trước khi tạo migration đầu tiên. Không tự ý thay đổi migration đã chạy trên môi trường dùng chung; hãy tạo migration mới.
